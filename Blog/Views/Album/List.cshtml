@model List<Blog.Models.Album>
@{
    ViewBag.Title = "List";
}

<div class="container">
    <div class="well">
        <h2>All Albums - @Html.ActionLink("Create", "Create", "Album", null, new { @class = "btn btn-warning" })</h2>
        <div class="row">
            <table class="table table-striped table-hover col-sm-12">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>@category.Id</td>
                            <td>@category.Name</td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", "Album", new { @id = category.Id }, null)
                                @Html.ActionLink("Delete", "Delete", "Album", new { @id = category.Id }, null)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


