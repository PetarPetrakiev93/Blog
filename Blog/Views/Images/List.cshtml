@model IEnumerable<Blog.Models.Images>
@{
    ViewBag.Title = "List";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New Page Content", "Create", null, new { @class = "btn btn-primary" })
</p>

<br />
<table class="table" style="width: 1200px;">
    <tr>
        <th>
            <b>Title</b>
        </th>
        <th>
            <b>Content</b>
        </th>
        <th>
            <b>Image</b>
        </th>
        <th>
            <b>Description</b>
        </th>
        <th>
            <b>Author</b>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(@item.Title, "Details", "Images", new { @id = item.Id}, null)
            </td>
            <td style="width: 500px;">
                @item.Contents
            </td>
            <td>
                <img src="/Images/RetrieveImage/@item.Id" alt="" height="200" />
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @item.Author.FullName
            </td>
        </tr>
    }

    
</table>